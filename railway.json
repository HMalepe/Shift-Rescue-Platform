{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$RAILWAY_SERVICE_NAME\" = \"web\" ]; then pnpm --filter @locum/web exec next start -H 0.0.0.0 -p ${PORT:-3000}; else pnpm --filter @locum/api start; fi'",
    "preDeployCommand": "sh -c 'if [ \"$RAILWAY_SERVICE_NAME\" != \"web\" ]; then pnpm --filter @locum/db migrate; fi'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
